#!/usr/bin/perl

use strict;
use warnings;
use 5.010;

# foreach loops


foreach my $i (5..10) {						# the beginiing the end values can be changed as per our wish
	print "I am a master in perl, yes : ", $i, "\n";
	
}


# multi- line comments

foreach (0..4) {						# the beginiing the end values can be changed as per our wish
	print "I am a master in perl, yes : ", $_, "\n";
	
}


# # C - style loops

# for (my $i = 0; $i < 101; $i++)	{				# the beginiing the end values can be changed as per our wish
	# print "I am a master in perl, yes : ", $i, "\n";
# }


# # C - style loops
# for (my $i = 0; $i < 101; $i+=10)	{				# the beginiing the end values can be changed as per our wish
	 # print "I am a master in perl, yes : ", $i, "\n";
 # }